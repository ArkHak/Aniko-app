<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>ForbiddenComment:AuthDto.kt$ProfileDto$ DTO профиля. `[TODO: verify live]` — полный набор полей. </ID>
    <ID>ForbiddenComment:CommonDto.kt$ApiCodeAware$* Общая обёртка Anixart: любой ответ несёт `code`, где `0` — успех. * * Сверено вживую: все запросы части A (`discover/watching`, `discover/interesting`, * `release/{id}`, `search/releases/{page}`) с валидным токеном вернули `code: 0`. * `[TODO: verify live]` — набор ненулевых кодов (кроме путей `auth/…`, см. `LoginViewModel`) * пока не подтверждён, т.к. не было воспроизведено ни одной ошибки сервера в этой сессии.</ID>
    <ID>ForbiddenComment:CommonDto.kt$PageableResponseDto$* `PageableResponse&lt;T&gt;` — постраничная выдача. * * Сверено вживую (`docs/api/samples/discover_watching_page0.json`, * `.../search_releases_page0_no_api_version_header.json`) и статически * (`network/response/PageableResponse.java`, поля с `@JsonProperty`): `content`, * `current_page`, `total_page_count`, `total_count` — все имена подтверждены 1:1. * В decompiled-модели `total_count` — `Long`, в живых сэмплах наблюдались только * значения, укладывающиеся в Int32 (например `88`); оставлено как `Int?` ради простоты, * `[TODO: verify live]`, если каталог когда-нибудь перевалит за ~2 млрд элементов.</ID>
    <ID>ForbiddenComment:FavoriteApi.kt$FavoriteApi$* `GET favorite/all/{page}?sort=&amp;filter_announce=` * * [sort] по умолчанию `1` — «сначала недавно добавленные». См. комментарий у * `ProfileListApi.myList` — та же (неочевидная, проверенная вживую только для списков по * статусу) семантика значения `sort` предполагается и здесь, так как это тот же query-параметр * и тот же REST-стиль API. `[TODO: verify live]` конкретно для избранного отдельно не * проверялось.</ID>
    <ID>ForbiddenComment:LibraryRepository.kt$LibraryRepository$* Добавляет релиз в список со статусом [status] — оптимистично локально, отправка на сервер * через [SyncQueueStore] (переживает офлайн, P4.T5). * * `[TODO: verify live]` (архитектурное допущение Фазы 6, не проверено живым запросом): * предполагаем, что статус в `profile/list/...` на сервере эксклюзивный, и повторный * `profile/list/add/{status}/{r_id}` с новым статусом сам заменяет старый — без явного * предварительного `removeFromList` со старым статусом. Если это окажется не так, при смене * статуса релиз останется числиться сразу в двух списках на сервере, и UI разойдётся с * официальным приложением. Сознательный трейдофф для MVP — один запрос вместо двух и без * гонки состояния между `remove`+`add`.</ID>
    <ID>ForbiddenComment:ProfileMapper.kt$* `Long` -&gt; `Int` для `privilegeLevel`/`watchedEpisodeCount` — в decompiled `Profile.java` оба * поля объявлены `long`, но на практике это счётчики небольшого порядка (уровень привилегий, * число просмотренных серий), поэтому в domain-модели [ProfileDetails] они сознательно сужены * до `Int` по заданию Фазы 7. `[TODO: verify live]` — если у активного пользователя счётчик * серий когда-нибудь превысит `Int.MAX_VALUE`, здесь произойдёт усечение.</ID>
    <ID>ForbiddenComment:ReleaseDto.kt$ReleaseDto$* DTO релиза. * * Сверено (R3, `docs/api/ENDPOINTS.md`): двумя независимыми способами — статически, по * `@JsonProperty` в decompiled `database/entity/release/Release.java` (Jackson, не Gson/Moshi — * значит имя JSON-поля равно либо аннотации, либо имени свойства as-is), и вживую curl'ом * (`GET discover/watching/0`, `GET release/{id}?extended_mode=true`, `POST search/releases/0`, * сэмплы в `docs/api/samples/`). Все поля ниже совпали в обоих источниках 1:1. * * Важные находки живой проверки: * - `image` в реальных ответах уже абсолютный URL (`https://s.anixmirai.com/...`), а не * относительный путь — `toAbsoluteUrl()` в мэппере это уже корректно обрабатывает (no-op * на строках с `http`), править не нужно. * - `status` (вложенный `{id, name}`) присутствует в `discover/watching` и `release/{id}`, но * ОТСУТСТВУЕТ в ответе `search/releases/{page}` — там есть только плоский `status_id`, * который **не совпадает** по значению с `status.id` в других ответах (наблюдалось * `status.id=1` при `status_id=0` у одного и того же релиза), так что использовать * `status_id` как замену вложенному `status` нельзя. Для результатов поиска * `Release.status` в домене будет `UNKNOWN` — задокументированное ограничение MVP. * - `id` в decompiled-модели объявлен как `Long` (Room PK), а не `Int`; во всех живых * сэмплах значения укладываются в Int32 (макс. наблюдалось `8013036` у `Interesting.id`). * Оставлено как `Int` (см. `ReleaseId` в `shared/model`) — `[TODO: verify live]` если когда-то * встретится реальный id вне диапазона Int32, конвертировать на `Long` придётся везде. * * `ignoreUnknownKeys = true` (см. `AnixJson`) по-прежнему гарантирует, что появление новых * полей в API не уронит парсинг. * * Фаза 7 (P7.T7-T13): добавлены поля расширенной карточки (`extended_mode=true`), сверены * с `docs/api/samples/release_186_extended.json`. Два расхождения с типом в * `ReleaseDetails` (`shared/model`), задокументированные здесь намеренно: * - `age_rating` в JSON — `Int` (живьём `5`), а не строка; таблица расшифровки значений нигде * не задокументирована (ни статически, ни вживую) — маппер (`ReleaseMapper.toReleaseDetails`) * конвертирует в `String` через `toString()`. `[TODO: verify live]` — расшифровать реальные * значения enum'а, если он вообще есть. * - `season` в JSON — `Int` (живьём `4`, порядковый номер сезона), маппер конвертирует в * `String` через `toString()`, т.к. `ReleaseDetails.season` типизирован строкой по заданию. * - Также присутствует легаси-дубль `comments_count` рядом с `comment_count` (оба `0` в сэмпле, * назначение различия не подтверждено) — используется только `comment_count`, как в задании.</ID>
    <ID>SwallowedException:SessionStore.kt$SessionStore$e: Exception</ID>
    <ID>TooGenericExceptionCaught:ApiCall.kt$e: Throwable</ID>
    <ID>TooGenericExceptionCaught:SessionStore.kt$SessionStore$e: Exception</ID>
    <ID>TooManyFunctions:LibraryRepository.kt$LibraryRepository</ID>
  </CurrentIssues>
</SmellBaseline>
